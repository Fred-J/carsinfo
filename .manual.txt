footer  //
    a clicking any button will trim its label
    b sets busy dict to true
    c and call the method matching that label

footer CLEAR //
    a clicking CLEAR sets dict busy to false & clears the display

footer INFO //
    a grabs the plate number input value and cleans it
    b saves plate number in dict plateLNum for display reactive response
    c check if some info are missing and calls the correct function
    d VehiclesDetailsColafterupdate hook calls functions according to what info is missing